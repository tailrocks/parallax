# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / PR
run-name: CI / PR · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  pull_request:
  workflow_dispatch:
    inputs:
      scope:
        description: Verification scope
        required: true
        default: affected
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-26.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      unit_ids: ${{ steps.plan.outputs.unit_ids }}
      full_units: ${{ steps.plan.outputs.full_units }}
      plan_digest: ${{ steps.plan.outputs.plan_digest }}
      excluded: ${{ steps.plan.outputs.excluded }}
      bun_matrix: ${{ steps.plan.outputs.bun_matrix }}
      docker_matrix: ${{ steps.plan.outputs.docker_matrix }}
      rust_matrix: ${{ steps.plan.outputs.rust_matrix }}
      # Presence-only no-work marker: `planned_no_work` is `true` or absent,
      # never `false`. Consumers MUST branch
      # `needs.plan.outputs.planned_no_work == 'true'` for the no-work path;
      # any other value (including absent) means the plan selected work.
      planned_no_work: ${{ steps.plan.outputs.planned_no_work }}
      no_work_reason: ${{ steps.plan.outputs.no_work_reason }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        id: runtime
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@eed474c4a1d9b071fd1b5de00c769c8997398e5a
        with:
          rev: eed474c4a1d9b071fd1b5de00c769c8997398e5a
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=eed474c4a1d9b071fd1b5de00c769c8997398e5a" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_PROVIDERS: github-hosted
          VELNOR_EVENT_TRUSTED: ${{ (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot'))) && 'true' || 'false' }}
          VELNOR_EXPECTED_WORK_FILE: .velnor-ci-expected-work/expected-work.json
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          mkdir -p .velnor-ci-expected-work
          velnor-workflow plan --config .github/ci/project.toml

      - name: Publish expected work
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-expected-work
          path: .velnor-ci-expected-work/expected-work.json
          if-no-files-found: error
          retention-days: 7
      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: eed474c4a1d9b071fd1b5de00c769c8997398e5a
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          policy_src="${VELNOR_WORKFLOW_PINNED_BINARY:-$src}"
          install -m 0755 "$policy_src" "$stage/velnor-workflow-policy"
          policy_revision="$("$stage/velnor-workflow-policy" --revision)"
          policy_closure="$("$stage/velnor-workflow-policy" --closure)"
          [[ "$policy_closure" == "${{ steps.runtime.outputs.closure }}" ]] || { echo "::error::policy runtime reports closure $policy_closure, expected ${{ steps.runtime.outputs.closure }}" >&2; exit 1; }
          policy_digest="$(sha256sum "$stage/velnor-workflow-policy" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg closure "${{ steps.runtime.outputs.closure }}" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" --arg policy_revision "$policy_revision" --arg policy_closure "$policy_closure" --arg policy_binary_sha256 "$policy_digest" '{repository: $repository, revision: $revision, closure: $closure, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256, policy_revision: $policy_revision, policy_closure: $policy_closure, policy_binary_sha256: $policy_binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-eed474c4a1d9b071fd1b5de00c769c8997398e5a-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  github-hosted-bun-ui:
    name: "Bun · Bun package (ui) · github-hosted — bun-ui"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"bun-ui"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-bun.yml
    with:
      unit: bun-ui
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      tool_version: "1.3.14"
      cache_paths: "~/.bun/install/cache"
      cache_key_files: |
        ui/package.json
        ui/bun.lock
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  github-hosted-docker-bench-otlp-fanout-maple:
    name: "Docker · Docker (bench/otlp-fanout/maple) · github-hosted — docker-bench-otlp-fanout-maple"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"docker-bench-otlp-fanout-maple"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-docker.yml
    with:
      unit: docker-bench-otlp-fanout-maple
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  github-hosted-rust-parallax-analysis:
    name: "Rust · parallax-analysis · github-hosted — rust-parallax-analysis"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-analysis"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-analysis
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "417b27aad74b"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-analysis/**/*.rs
        crates/parallax-analysis/benches/**
        crates/parallax-analysis/examples/**
        crates/parallax-analysis/src/**
        crates/parallax-analysis/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-analysis"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-model,rust-parallax-proto,rust-parallax-semconv"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-api:
    name: "Rust · parallax-api · github-hosted — rust-parallax-api"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-api"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-api
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "2673ddf71dd2"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-analysis/**/*.rs
        crates/parallax-analysis/benches/**
        crates/parallax-analysis/examples/**
        crates/parallax-analysis/src/**
        crates/parallax-analysis/tests/**
        crates/parallax-api/**/*.rs
        crates/parallax-api/benches/**
        crates/parallax-api/examples/**
        crates/parallax-api/src/**
        crates/parallax-api/src/lib.rs
        crates/parallax-api/tests/**
        crates/parallax-evidence/**/*.rs
        crates/parallax-evidence/benches/**
        crates/parallax-evidence/examples/**
        crates/parallax-evidence/fixtures/bundle-v1-golden.json
        crates/parallax-evidence/src/**
        crates/parallax-evidence/tests/**
        crates/parallax-metadata/**/*.rs
        crates/parallax-metadata/benches/**
        crates/parallax-metadata/examples/**
        crates/parallax-metadata/src/**
        crates/parallax-metadata/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        crates/parallax-test-support/**/*.rs
        crates/parallax-test-support/benches/**
        crates/parallax-test-support/examples/**
        crates/parallax-test-support/src/**
        crates/parallax-test-support/tests/**
        mise.lock
        schema/evidence-bundle.v1.schema.json
        schema/evidence-bundle.v2.schema.json
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-api"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-analysis,rust-parallax-evidence,rust-parallax-metadata,rust-parallax-storage,rust-parallax-test-support"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-cli:
    name: "Rust · parallax-cli · github-hosted — rust-parallax-cli"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-cli"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-cli
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest bun"
      mbx_enabled: true
      mbx_compat: "5744d53d57b5"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
        ui/bun.lock
        ui/package.json
      mbx_freshness_files: |
        crates/parallax-analysis/**/*.rs
        crates/parallax-analysis/benches/**
        crates/parallax-analysis/examples/**
        crates/parallax-analysis/src/**
        crates/parallax-analysis/tests/**
        crates/parallax-api/**/*.rs
        crates/parallax-api/benches/**
        crates/parallax-api/examples/**
        crates/parallax-api/src/**
        crates/parallax-api/src/lib.rs
        crates/parallax-api/tests/**
        crates/parallax-cli/**/*.rs
        crates/parallax-cli/benches/**
        crates/parallax-cli/build.rs
        crates/parallax-cli/examples/**
        crates/parallax-cli/src/**
        crates/parallax-cli/tests/**
        crates/parallax-evidence/**/*.rs
        crates/parallax-evidence/benches/**
        crates/parallax-evidence/examples/**
        crates/parallax-evidence/fixtures/bundle-v1-golden.json
        crates/parallax-evidence/src/**
        crates/parallax-evidence/tests/**
        crates/parallax-greptime/**/*.rs
        crates/parallax-greptime/benches/**
        crates/parallax-greptime/examples/**
        crates/parallax-greptime/src/**
        crates/parallax-greptime/tests/**
        crates/parallax-ingest/**/*.rs
        crates/parallax-ingest/benches/**
        crates/parallax-ingest/examples/**
        crates/parallax-ingest/src/**
        crates/parallax-ingest/tests/**
        crates/parallax-metadata/**/*.rs
        crates/parallax-metadata/benches/**
        crates/parallax-metadata/examples/**
        crates/parallax-metadata/src/**
        crates/parallax-metadata/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-server/**/*.rs
        crates/parallax-server/benches/**
        crates/parallax-server/build.rs
        crates/parallax-server/examples/**
        crates/parallax-server/src/**
        crates/parallax-server/src/ingest_health.rs
        crates/parallax-server/tests/**
        crates/parallax-spool/**/*.rs
        crates/parallax-spool/benches/**
        crates/parallax-spool/examples/**
        crates/parallax-spool/src/**
        crates/parallax-spool/src/spool.rs
        crates/parallax-spool/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        crates/parallax-test-support/**/*.rs
        crates/parallax-test-support/benches/**
        crates/parallax-test-support/examples/**
        crates/parallax-test-support/src/**
        crates/parallax-test-support/tests/**
        mise.lock
        schema/evidence-bundle.v1.schema.json
        schema/evidence-bundle.v2.schema.json
        ui/**
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-cli"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-evidence,rust-parallax-metadata,rust-parallax-model,rust-parallax-redaction,rust-parallax-semconv,rust-parallax-server,rust-parallax-spool,rust-parallax-storage,bun-ui"
      unit_admission: github-hosted
  github-hosted-rust-parallax-evidence:
    name: "Rust · parallax-evidence · github-hosted — rust-parallax-evidence"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-evidence"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-evidence
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "418f8638ba86"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-analysis/**/*.rs
        crates/parallax-analysis/benches/**
        crates/parallax-analysis/examples/**
        crates/parallax-analysis/src/**
        crates/parallax-analysis/tests/**
        crates/parallax-evidence/**/*.rs
        crates/parallax-evidence/benches/**
        crates/parallax-evidence/examples/**
        crates/parallax-evidence/fixtures/bundle-v1-golden.json
        crates/parallax-evidence/src/**
        crates/parallax-evidence/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        mise.lock
        schema/evidence-bundle.v1.schema.json
        schema/evidence-bundle.v2.schema.json
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-evidence"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-analysis,rust-parallax-model,rust-parallax-redaction"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-greptime:
    name: "Rust · parallax-greptime · github-hosted — rust-parallax-greptime"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-greptime"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-greptime
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: b84f73ea31c3
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-greptime/**/*.rs
        crates/parallax-greptime/benches/**
        crates/parallax-greptime/examples/**
        crates/parallax-greptime/src/**
        crates/parallax-greptime/tests/**
        crates/parallax-ingest/**/*.rs
        crates/parallax-ingest/benches/**
        crates/parallax-ingest/examples/**
        crates/parallax-ingest/src/**
        crates/parallax-ingest/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-greptime"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-ingest,rust-parallax-model,rust-parallax-proto,rust-parallax-semconv,rust-parallax-storage"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-ingest:
    name: "Rust · parallax-ingest · github-hosted — rust-parallax-ingest"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-ingest"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-ingest
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: b4d6536134c0
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-ingest/**/*.rs
        crates/parallax-ingest/benches/**
        crates/parallax-ingest/examples/**
        crates/parallax-ingest/src/**
        crates/parallax-ingest/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-ingest"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-model,rust-parallax-proto,rust-parallax-semconv"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-mcp:
    name: "Rust · parallax-mcp · github-hosted — rust-parallax-mcp"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-mcp"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-mcp
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: f16c11de9052
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-analysis/**/*.rs
        crates/parallax-analysis/benches/**
        crates/parallax-analysis/examples/**
        crates/parallax-analysis/src/**
        crates/parallax-analysis/tests/**
        crates/parallax-evidence/**/*.rs
        crates/parallax-evidence/benches/**
        crates/parallax-evidence/examples/**
        crates/parallax-evidence/fixtures/bundle-v1-golden.json
        crates/parallax-evidence/src/**
        crates/parallax-evidence/tests/**
        crates/parallax-mcp/**/*.rs
        crates/parallax-mcp/benches/**
        crates/parallax-mcp/examples/**
        crates/parallax-mcp/src/**
        crates/parallax-mcp/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        mise.lock
        schema/evidence-bundle.v1.schema.json
        schema/evidence-bundle.v2.schema.json
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-mcp"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-parallax-evidence
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test"
  github-hosted-rust-parallax-metadata:
    name: "Rust · parallax-metadata · github-hosted — rust-parallax-metadata"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-metadata"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-metadata
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "9401f5324ce8"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-metadata/**/*.rs
        crates/parallax-metadata/benches/**
        crates/parallax-metadata/examples/**
        crates/parallax-metadata/src/**
        crates/parallax-metadata/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-metadata"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-model,rust-parallax-redaction,rust-parallax-semconv,rust-parallax-storage"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-model:
    name: "Rust · parallax-model · github-hosted — rust-parallax-model"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-model"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-model
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "0cda3448c8df"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-model"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-proto:
    name: "Rust · parallax-proto · github-hosted — rust-parallax-proto"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-proto"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-proto
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: b6dc5976c9b2
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-proto"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-parallax-semconv
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-redaction:
    name: "Rust · parallax-redaction · github-hosted — rust-parallax-redaction"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-redaction"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-redaction
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: b6f8b3cf320e
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-redaction"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-semconv:
    name: "Rust · parallax-semconv · github-hosted — rust-parallax-semconv"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-semconv"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-semconv
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "4757a5b53ecb"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-semconv"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-sentry-proxy:
    name: "Rust · parallax-sentry-proxy · github-hosted — rust-parallax-sentry-proxy"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-sentry-proxy"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-sentry-proxy
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: d899cbed418a
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-ingest/**/*.rs
        crates/parallax-ingest/benches/**
        crates/parallax-ingest/examples/**
        crates/parallax-ingest/src/**
        crates/parallax-ingest/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-sentry-proxy/**/*.rs
        crates/parallax-sentry-proxy/benches/**
        crates/parallax-sentry-proxy/examples/**
        crates/parallax-sentry-proxy/src/**
        crates/parallax-sentry-proxy/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-sentry-proxy"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-parallax-ingest
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-server:
    name: "Rust · parallax-server · github-hosted — rust-parallax-server"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-server"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-server
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest bun"
      mbx_enabled: true
      mbx_compat: "5496d9b1eb6e"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
        ui/bun.lock
        ui/package.json
      mbx_freshness_files: |
        crates/parallax-analysis/**/*.rs
        crates/parallax-analysis/benches/**
        crates/parallax-analysis/examples/**
        crates/parallax-analysis/src/**
        crates/parallax-analysis/tests/**
        crates/parallax-api/**/*.rs
        crates/parallax-api/benches/**
        crates/parallax-api/examples/**
        crates/parallax-api/src/**
        crates/parallax-api/src/lib.rs
        crates/parallax-api/tests/**
        crates/parallax-evidence/**/*.rs
        crates/parallax-evidence/benches/**
        crates/parallax-evidence/examples/**
        crates/parallax-evidence/fixtures/bundle-v1-golden.json
        crates/parallax-evidence/src/**
        crates/parallax-evidence/tests/**
        crates/parallax-greptime/**/*.rs
        crates/parallax-greptime/benches/**
        crates/parallax-greptime/examples/**
        crates/parallax-greptime/src/**
        crates/parallax-greptime/tests/**
        crates/parallax-ingest/**/*.rs
        crates/parallax-ingest/benches/**
        crates/parallax-ingest/examples/**
        crates/parallax-ingest/src/**
        crates/parallax-ingest/tests/**
        crates/parallax-metadata/**/*.rs
        crates/parallax-metadata/benches/**
        crates/parallax-metadata/examples/**
        crates/parallax-metadata/src/**
        crates/parallax-metadata/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-server/**/*.rs
        crates/parallax-server/benches/**
        crates/parallax-server/build.rs
        crates/parallax-server/examples/**
        crates/parallax-server/src/**
        crates/parallax-server/src/ingest_health.rs
        crates/parallax-server/tests/**
        crates/parallax-spool/**/*.rs
        crates/parallax-spool/benches/**
        crates/parallax-spool/examples/**
        crates/parallax-spool/src/**
        crates/parallax-spool/src/spool.rs
        crates/parallax-spool/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        crates/parallax-test-support/**/*.rs
        crates/parallax-test-support/benches/**
        crates/parallax-test-support/examples/**
        crates/parallax-test-support/src/**
        crates/parallax-test-support/tests/**
        mise.lock
        schema/evidence-bundle.v1.schema.json
        schema/evidence-bundle.v2.schema.json
        ui/**
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-server"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-analysis,rust-parallax-api,rust-parallax-evidence,rust-parallax-greptime,rust-parallax-ingest,rust-parallax-metadata,rust-parallax-proto,rust-parallax-semconv,rust-parallax-spool,rust-parallax-storage,rust-parallax-test-support,bun-ui"
      unit_admission: github-hosted
  github-hosted-rust-parallax-spool:
    name: "Rust · parallax-spool · github-hosted — rust-parallax-spool"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-spool"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-spool
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "011f8cf7f2b5"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-spool/**/*.rs
        crates/parallax-spool/benches/**
        crates/parallax-spool/examples/**
        crates/parallax-spool/src/**
        crates/parallax-spool/src/spool.rs
        crates/parallax-spool/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-spool"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-storage:
    name: "Rust · parallax-storage · github-hosted — rust-parallax-storage"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-storage"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-storage
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "9ebe2905ba1c"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-storage"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-model,rust-parallax-proto,rust-parallax-semconv"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-test-support:
    name: "Rust · parallax-test-support · github-hosted — rust-parallax-test-support"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-test-support"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-test-support
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "4f9a3e9477a8"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-metadata/**/*.rs
        crates/parallax-metadata/benches/**
        crates/parallax-metadata/examples/**
        crates/parallax-metadata/src/**
        crates/parallax-metadata/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        crates/parallax-test-support/**/*.rs
        crates/parallax-test-support/benches/**
        crates/parallax-test-support/examples/**
        crates/parallax-test-support/src/**
        crates/parallax-test-support/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-test-support"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-parallax-metadata,rust-parallax-model,rust-parallax-proto,rust-parallax-semconv,rust-parallax-storage"
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-parallax-xtask:
    name: "Rust · parallax-xtask · github-hosted — rust-parallax-xtask"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-parallax-xtask"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-parallax-xtask
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_enabled: true
      mbx_compat: "77452e3c07de"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/parallax-analysis/**/*.rs
        crates/parallax-analysis/benches/**
        crates/parallax-analysis/examples/**
        crates/parallax-analysis/src/**
        crates/parallax-analysis/tests/**
        crates/parallax-api/**/*.rs
        crates/parallax-api/benches/**
        crates/parallax-api/examples/**
        crates/parallax-api/src/**
        crates/parallax-api/src/lib.rs
        crates/parallax-api/tests/**
        crates/parallax-evidence/**/*.rs
        crates/parallax-evidence/benches/**
        crates/parallax-evidence/examples/**
        crates/parallax-evidence/fixtures/bundle-v1-golden.json
        crates/parallax-evidence/src/**
        crates/parallax-evidence/tests/**
        crates/parallax-metadata/**/*.rs
        crates/parallax-metadata/benches/**
        crates/parallax-metadata/examples/**
        crates/parallax-metadata/src/**
        crates/parallax-metadata/tests/**
        crates/parallax-model/**/*.rs
        crates/parallax-model/benches/**
        crates/parallax-model/examples/**
        crates/parallax-model/src/**
        crates/parallax-model/tests/**
        crates/parallax-proto/**/*.rs
        crates/parallax-proto/benches/**
        crates/parallax-proto/examples/**
        crates/parallax-proto/src/**
        crates/parallax-proto/tests/**
        crates/parallax-redaction/**/*.rs
        crates/parallax-redaction/benches/**
        crates/parallax-redaction/examples/**
        crates/parallax-redaction/src/**
        crates/parallax-redaction/tests/**
        crates/parallax-semconv/**/*.rs
        crates/parallax-semconv/benches/**
        crates/parallax-semconv/examples/**
        crates/parallax-semconv/src/**
        crates/parallax-semconv/tests/**
        crates/parallax-storage/**/*.rs
        crates/parallax-storage/benches/**
        crates/parallax-storage/examples/**
        crates/parallax-storage/src/**
        crates/parallax-storage/tests/**
        crates/parallax-test-support/**/*.rs
        crates/parallax-test-support/benches/**
        crates/parallax-test-support/examples/**
        crates/parallax-test-support/src/**
        crates/parallax-test-support/tests/**
        crates/parallax-xtask/**/*.rs
        crates/parallax-xtask/benches/**
        crates/parallax-xtask/examples/**
        crates/parallax-xtask/fixtures/typescript-import-parity.json
        crates/parallax-xtask/src/**
        crates/parallax-xtask/tests/**
        docs/research/decisions/evidence-bundle-contract.md
        mise.lock
        schema/evidence-bundle.v1.schema.json
        schema/evidence-bundle.v2.schema.json
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/parallax-xtask"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-parallax-api
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test,doctest"
  github-hosted-rust-policy:
    name: "Rust · Rust dependency policy · github-hosted — rust-policy"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-policy"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: a6b5791cddd3
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        deny.toml
      mbx_freshness_files: |
        .cargo/audit.toml
        .cargo/deny.toml
        Cargo.lock
        audit.toml
        deny.toml
      cargo_bin_tools: cargo-deny
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  ci-required:
    name: ci-required
    if: ${{ !cancelled() }}
    needs: [plan, github-hosted-bun-ui, github-hosted-docker-bench-otlp-fanout-maple, github-hosted-rust-parallax-analysis, github-hosted-rust-parallax-api, github-hosted-rust-parallax-cli, github-hosted-rust-parallax-evidence, github-hosted-rust-parallax-greptime, github-hosted-rust-parallax-ingest, github-hosted-rust-parallax-mcp, github-hosted-rust-parallax-metadata, github-hosted-rust-parallax-model, github-hosted-rust-parallax-proto, github-hosted-rust-parallax-redaction, github-hosted-rust-parallax-semconv, github-hosted-rust-parallax-sentry-proxy, github-hosted-rust-parallax-server, github-hosted-rust-parallax-spool, github-hosted-rust-parallax-storage, github-hosted-rust-parallax-test-support, github-hosted-rust-parallax-xtask, github-hosted-rust-policy]
    runs-on: ubuntu-26.04
    timeout-minutes: 5
    steps:
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-eed474c4a1d9b071fd1b5de00c769c8997398e5a-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: eed474c4a1d9b071fd1b5de00c769c8997398e5a
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$")) and (.policy_binary_sha256 | test("^[0-9a-f]{64}$")) and (.closure | test("^[0-9a-f]{64}$")) and (.policy_closure | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
          expected="$(jq -er '.policy_binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow-policy | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::policy runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        env:
          EXPECTED_REVISION: eed474c4a1d9b071fd1b5de00c769c8997398e5a
        run: |
          set -euo pipefail
          home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$home/bin/velnor-workflow"
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow-policy "$home/bin/velnor-workflow-policy"
          expected_closure="$(jq -er '.policy_closure' .velnor-workflow-runtime/manifest.json)"
          reported="$("$home/bin/velnor-workflow-policy" --closure)"
          [[ "$reported" == "$expected_closure" ]] || { echo "::error::policy runtime reports closure $reported, expected $expected_closure" >&2; exit 1; }
          echo "$home/bin" >> "$GITHUB_PATH"
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$home/bin/velnor-workflow-policy" >> "$GITHUB_ENV"
      - name: Download expected work
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-expected-work
          path: .velnor-ci-expected-work
      - name: Download reported unit results
        # A no-work plan runs no unit jobs, so zero result artifacts is the
        # expected case there — and the aggregate fails a real-work plan with
        # zero records anyway. Tolerate the empty download; never the verdict.
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: velnor-result-*
          merge-multiple: true
          path: .velnor-ci-results
      - name: Collect reported unit results
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          mkdir -p .velnor-ci-results
          files=(.velnor-ci-results/result-*.json)
          for file in "${files[@]}"; do
            if jq -e 'any(.results[]?; has("reused_from"))' "$file" >/dev/null; then
              echo "::error::$file carries reused_from without a validate_reuse decision; render emits no reused results" >&2
              exit 1
            fi
          done
          if (( ${#files[@]} == 0 )); then
            printf '{"results":[]}\n' > .velnor-ci-results.json
          else
            jq -s '{results: ([.[].results // empty] | add // [])}' "${files[@]}" > .velnor-ci-results.json
          fi
          echo "collected $(jq '.results | length' .velnor-ci-results.json) reported result(s) from ${#files[@]} record file(s)"
      - name: Score expected work against reported results
        env:
          BASE_SHA: ${{ needs.plan.outputs.base_sha }}
          HEAD_SHA: ${{ needs.plan.outputs.head_sha }}
        shell: bash
        run: |
          set -euo pipefail
          velnor-workflow aggregate --expected .velnor-ci-expected-work/expected-work.json --results .velnor-ci-results.json
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          PLAN_DIGEST: ${{ needs.plan.outputs.plan_digest }}
          EXCLUDED: ${{ needs.plan.outputs.excluded }}
          EXPECTED_CALLERS: "[{\"job_id\":\"github-hosted-bun-ui\",\"provider\":\"github-hosted\",\"unit_id\":\"bun-ui\"},{\"job_id\":\"github-hosted-docker-bench-otlp-fanout-maple\",\"provider\":\"github-hosted\",\"unit_id\":\"docker-bench-otlp-fanout-maple\"},{\"job_id\":\"github-hosted-rust-parallax-analysis\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-analysis\"},{\"job_id\":\"github-hosted-rust-parallax-api\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-api\"},{\"job_id\":\"github-hosted-rust-parallax-cli\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-cli\"},{\"job_id\":\"github-hosted-rust-parallax-evidence\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-evidence\"},{\"job_id\":\"github-hosted-rust-parallax-greptime\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-greptime\"},{\"job_id\":\"github-hosted-rust-parallax-ingest\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-ingest\"},{\"job_id\":\"github-hosted-rust-parallax-mcp\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-mcp\"},{\"job_id\":\"github-hosted-rust-parallax-metadata\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-metadata\"},{\"job_id\":\"github-hosted-rust-parallax-model\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-model\"},{\"job_id\":\"github-hosted-rust-parallax-proto\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-proto\"},{\"job_id\":\"github-hosted-rust-parallax-redaction\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-redaction\"},{\"job_id\":\"github-hosted-rust-parallax-semconv\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-semconv\"},{\"job_id\":\"github-hosted-rust-parallax-sentry-proxy\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-sentry-proxy\"},{\"job_id\":\"github-hosted-rust-parallax-server\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-server\"},{\"job_id\":\"github-hosted-rust-parallax-spool\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-spool\"},{\"job_id\":\"github-hosted-rust-parallax-storage\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-storage\"},{\"job_id\":\"github-hosted-rust-parallax-test-support\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-test-support\"},{\"job_id\":\"github-hosted-rust-parallax-xtask\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-parallax-xtask\"},{\"job_id\":\"github-hosted-rust-policy\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-policy\"}]"
          PROVIDER_ADMITTED_GITHUB_HOSTED: ${{ true }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "$PLAN_DIGEST" ]]; then
            echo "plan did not freeze a plan digest: the expected set has no identity" >&2
            exit 1
          fi
          echo "verdict binds plan digest $PLAN_DIGEST"
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          plan_expects() {
            [[ "$(jq -r --arg unit "$1" --arg provider "$2" '[.[] | select(.unit_id == $unit) | .providers[] | select(. == $provider)] | length' <<<"$SELECTED_UNITS")" -gt 0 ]]
          }
          if ! jq -e --argjson expected "$EXPECTED_CALLERS" '($expected | type == "array") and all($expected[]; type == "object" and (.unit_id | type) == "string" and (.provider | type) == "string" and (.job_id | type) == "string")' -n; then
            echo "generated required-caller contract is malformed" >&2
            exit 1
          fi
          if ! jq -e --argjson expected "$EXPECTED_CALLERS" 'type == "array" and (map(.unit_id) | unique | length) == length and all(.[]; . as $entry | ($entry | type) == "object" and ($entry.unit_id | type) == "string" and ($entry.unit_id | length) > 0 and ($entry.providers | type) == "array" and ($entry.providers | length) > 0 and (($entry.providers | map(type == "string" and length > 0) | all)) and (($entry.providers | unique | length) == ($entry.providers | length)) and all($entry.providers[]; . as $provider | any($expected[]; .unit_id == $entry.unit_id and .provider == $provider)))' <<<"$SELECTED_UNITS" >/dev/null; then
            echo "plan selected-unit output contains an unknown, duplicate, empty, or unmapped obligation" >&2
            exit 1
          fi
          if ! jq -e 'type == "object"' <<<"$NEEDS_JSON" >/dev/null; then
            echo "workflow needs output is malformed" >&2
            exit 1
          fi
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          if plan_expects "bun-ui" "github-hosted"; then
            result="$(result_for_job github-hosted-bun-ui)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-bun-ui was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-bun-ui was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-bun-ui did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-bun-ui ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-bun-ui)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-bun-ui succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-bun-ui ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "docker-bench-otlp-fanout-maple" "github-hosted"; then
            result="$(result_for_job github-hosted-docker-bench-otlp-fanout-maple)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-docker-bench-otlp-fanout-maple was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-docker-bench-otlp-fanout-maple was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-docker-bench-otlp-fanout-maple did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-docker-bench-otlp-fanout-maple ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-docker-bench-otlp-fanout-maple)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-docker-bench-otlp-fanout-maple succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-docker-bench-otlp-fanout-maple ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-analysis" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-analysis)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-analysis was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-analysis was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-analysis did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-analysis ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-analysis)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-analysis succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-analysis ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-api" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-api)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-api was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-api was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-api did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-api ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-api)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-api succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-api ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-cli" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-cli)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-cli was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-cli was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-cli did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-cli ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-cli)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-cli succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-cli ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-evidence" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-evidence)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-evidence was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-evidence was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-evidence did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-evidence ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-evidence)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-evidence succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-evidence ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-greptime" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-greptime)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-greptime was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-greptime was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-greptime did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-greptime ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-greptime)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-greptime succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-greptime ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-ingest" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-ingest)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-ingest was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-ingest was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-ingest did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-ingest ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-ingest)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-ingest succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-ingest ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-mcp" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-mcp)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-mcp was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-mcp was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-mcp did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-mcp ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-mcp)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-mcp succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-mcp ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-metadata" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-metadata)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-metadata was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-metadata was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-metadata did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-metadata ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-metadata)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-metadata succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-metadata ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-model" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-model)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-model was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-model was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-model did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-model ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-model)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-model succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-model ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-proto" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-proto)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-proto was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-proto was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-proto did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-proto ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-proto)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-proto succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-proto ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-redaction" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-redaction)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-redaction was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-redaction was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-redaction did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-redaction ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-redaction)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-redaction succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-redaction ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-semconv" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-semconv)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-semconv was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-semconv was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-semconv did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-semconv ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-semconv)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-semconv succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-semconv ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-sentry-proxy" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-sentry-proxy)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-sentry-proxy was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-sentry-proxy was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-sentry-proxy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-sentry-proxy ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-sentry-proxy)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-sentry-proxy succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-sentry-proxy ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-server" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-server)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-server was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-server was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-server did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-server ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-server)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-server succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-server ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-spool" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-spool)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-spool was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-spool was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-spool did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-spool ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-spool)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-spool succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-spool ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-storage" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-storage)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-storage was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-storage was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-storage did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-storage ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-storage)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-storage succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-storage ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-test-support" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-test-support)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-test-support was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-test-support was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-test-support did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-test-support ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-test-support)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-test-support succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-test-support ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-parallax-xtask" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-parallax-xtask)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-parallax-xtask was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-parallax-xtask was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-parallax-xtask did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-parallax-xtask ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-parallax-xtask)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-parallax-xtask succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-parallax-xtask ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-policy" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-policy)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-policy was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-policy was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-policy ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-policy)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-policy succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-policy ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ !cancelled() }}
    needs: [ci-required]
    runs-on: ubuntu-26.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
