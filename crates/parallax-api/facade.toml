schema_version = 1

[roots]
"lib.rs" = [
    "struct ApiContext",
    "struct Mutation",
    "struct Query",
    "use alert_preview::{AlertPreviewData, AlertPreviewGroupData, AlertPreviewPointData, AlertPreviewer ,}",
    "use memo::RequestMemo",
    "use schema::{Schema, build_schema, check_query_limits, execute, export_schema_sdl, normalize_schema_sdl ,}",
]
