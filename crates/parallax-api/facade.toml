schema_version = 1

[roots]
"lib.rs" = [
    "struct ApiContext",
    "struct Mutation",
    "struct Query",
    "use memo::RequestMemo",
    "use query_limits::check_query_limits",
    "use schema::{Schema, build_schema, execute, export_schema_sdl, normalize_schema_sdl}",
]
