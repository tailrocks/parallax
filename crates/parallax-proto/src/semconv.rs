//! Compatibility re-export for semantic-convention constants.
//!
//! New code should depend on `parallax-semconv` directly. This module remains
//! while Plan 119 migrates existing consumers without changing wire names.

pub use parallax_semconv::*;
