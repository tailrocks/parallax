schema_version = 1

[roots]
"lib.rs" = [
    "mod adapter",
    "mod metadata",
    "mod prune",
    "mod projections",
    "use prune::{MetadataPruneStore, PruneAuthorization, PruneClass, PruneEstimate, PruneExclusion, PruneExclusionKind, PruneExecutionMode, PruneExecutionRequest, PruneItem, PrunePlan, PrunePlanError, PrunePlanLimits, PruneSnapshot, PruneStore}",
    "use parallax_model as model",
]
