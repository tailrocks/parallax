schema_version = 1

[roots]
"lib.rs" = [
    "mod adapter",
    "mod metadata",
    "mod projections",
    "use parallax_model as model",
    "use prune::{MetadataPruneJournalStore, MetadataPruneStore, PRUNE_JOURNAL_ERROR_MAX_BYTES, PruneAuthorization, PruneClass, PruneEstimate, PruneExclusion, PruneExclusionKind, PruneExecutionMode, PruneExecutionRequest, PruneItem, PruneJournal, PruneJournalStep, PruneJournalStepState, PrunePlan, PrunePlanError, PrunePlanLimits, PruneSnapshot, PruneStepStart, PruneStore ,}",
]
