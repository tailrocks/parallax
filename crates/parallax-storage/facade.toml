schema_version = 1

[roots]
"lib.rs" = [
    'cfg (any (test, feature = "test-support")) mod conformance',
    'cfg (any (test, feature = "test-support")) mod memory',
    "mod adapter",
    "mod greptime",
    "mod metadata",
    "mod model",
    "mod spool",
]
