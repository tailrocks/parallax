/** Short import path for time-range domain values (printWidth-friendly). */
export {
  RANGE_PRESETS,
  DEFAULT_RANGE_KEY,
  rangeSearchSchema,
  type ResolvedRange,
  resolvePreset,
  customRange,
  resolveRangeSearch,
  updateRangeSearch,
  rangeLinkSearch,
  mergeRangeSearch,
  formatRangeLabel,
} from "@/domain/time-range/range"
