# Implementation Plans

Generated by the improve skill on 2026-07-03. Execute in order unless dependencies say otherwise. Each executor: read the plan fully before starting, honor STOP conditions, and update your row when done.

Research target: bring Parallax UI/UX close to the visual reference's look and feel while preserving Parallax behavior, Vite/TanStack Router, Tailwind v4, Bun, and shadcn/base-vega components.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish visual reference-style theme tokens | P1 | M | - | DONE |
| 002 | Rebuild app shell around dark product surface | P1 | M | 001 | DONE |
| 003 | Convert data pages to dense observability panels | P1 | L | 001, 002 | DONE |
| 004 | Keep branded shell visible during loading/errors | P1 | S | 001, 002 | DONE |

## Dependency Notes

- 002 depends on 001 because the shell must consume shared brand tokens, radii, shadows, and chart colors instead of page-local raw classes.
- 003 depends on 001 and 002 because issue, run, trace, log, dashboard, service, and SQL pages should compose inside the same dark shell and token system.
- 004 depends on 001 and 002 because current API failure renders TanStack Router's default error UI and hides the shell; the error surface must use the new shell/cards.

## Verification Baseline

Run UI commands from `ui/`:

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Typecheck | `rtk bun run typecheck` | exit 0, no TS errors |
| Lint | `rtk bun run lint` | exit 0 |
| Tests | `rtk bun run test` | exit 0 |
| Build | `rtk bun run build` | exit 0 |
| Dev preview | `rtk bun run dev` | Vite reports `http://localhost:3000/` |

Known current render issue: with no API server at `127.0.0.1:4000`, `/issues` shows only TanStack's default `Something went wrong!` boundary and `parallax api unreachable (502)`. Plan 004 fixes this UI failure mode.

## Sources Used

- visual reference homepage, fetched 2026-07-03: visual reference homepage (URL omitted)
- visual reference docs index, fetched 2026-07-03: visual reference docs index (URL omitted)
- visual reference Open Graph image, fetched 2026-07-03: visual reference Open Graph image (URL omitted)
- Parallax UI source at commit `8dde008`.

## Findings Considered And Rejected

- Copying the visual reference's exact product labels and routes: rejected. User asked for look/feel, not product semantics; Parallax must remain context engine for issues, runs, traces, logs, services, dashboards, SQL.
- Switching framework or component system: rejected. Repo rules require Bun, Tailwind, TanStack, and shadcn; user explicitly said keep Tailwind and shadcn.
- Importing visual reference proprietary CSS wholesale: rejected. Build a Parallax-owned design system inspired by observed patterns; do not vendor another product's implementation.
